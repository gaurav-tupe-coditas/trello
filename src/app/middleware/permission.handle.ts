import type { NextFunction, Request, Response } from "express";

const permissionChecker = (permissionName:string)=>async(req:Request,res:Response,next:NextFunction)=>{

    try {
        
    } catch (error) {
        throw error
    }
}
